# URL de Neon (misma que en el servidor). No subir a Git.
# Copia desde: ssh usuario@servidor "grep DATABASE_URL ~/arandano/.env.local"
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
