# =============================================================================
# Producción OVH/VPS — copiar a la raíz del proyecto como .env.local
# cp deploy/ovh/env.example .env.local
# =============================================================================

DB_MODE=sqlite
ADMIN_PASSWORD=

NEXTAUTH_URL=https://TU-DOMINIO.com
NEXTAUTH_SECRET=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

FOOTBALL_DATA_API_TOKEN=

NODE_ENV=production
PORT=3000

# PM2 (ecosystem.config.js) define PROJECT_ROOT, DATA_DIR y DATABASE_PATH.
# La base de datos vive en: data/arandano.db (persistente en el servidor).
