# =============================================================================
# Desarrollo local — copiar a .env (gitignored)
# =============================================================================

DB_MODE=postgres
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

NEXTAUTH_SECRET=genera-un-secreto-aleatorio-largo

GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu-client-secret

FOOTBALL_DATA_API_TOKEN=tu-token

# =============================================================================
# URLs — desarrollo usa .env.development (localhost)
# Producción en servidor: deploy/ovh/env.server → ~/arandano/.env.local
# =============================================================================
